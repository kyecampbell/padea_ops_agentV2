"""Tool belt package.

The orchestrator's tools: typed results, read (query), data writes, email,
escalation, whitelisted scripts, and the casebook memory. Every tool returns a
typed outcome and never raises at the agent (see `results.py`).
"""

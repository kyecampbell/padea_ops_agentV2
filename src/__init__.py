"""Padea Operations Agent — source root.

Top-level package for the orchestrator agent, its tool belt, and the database,
Gmail, and memory adapters. Imports are absolute from the project root, e.g.
`from src.tools.query import ...`.
"""

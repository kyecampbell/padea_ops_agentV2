"""Configuration package.

Responsibility: expose the loaded application settings and the always-on policy
handbook to the rest of the codebase.

TODO: re-export the settings singleton (e.g. `from config.settings import settings`)
once the loader in settings.py is implemented.
"""
